GAS_DATABASE=/var/lib/gas/gas.sqlite3
# Only trust the loopback proxy. It MUST overwrite X-Real-IP with the connecting client IP.
GAS_TRUSTED_PROXIES=127.0.0.1
# Optional custom question catalog, loaded at process startup:
# GAS_QUESTIONS=/opt/gas/server/content/questions.json
